#!/usr/bin/ruby

require "rubygems"
require "active_resource"
require "shelltoad"

command = ARGV.shift

case command
when "errors", "ers", nil
  Shelltoad::Error.find(:all).each do |error|
    unless error.rails_env == "development"
      puts error.to_s
    end
  end
when "error"
  puts Shelltoad::Error.magic_find(ARGV.shift) || "Not found"
when /^[\d]/
  puts Shelltoad::Error.magic_find(command) || "Not Found"
when "commit"
  if error = Shelltoad::Error.magic_find(ARGV.shift)
    message = <<-EOI.gsub(/`/, "'")
#{Hoptoad.site}/errors/#{error.id}

#{error.error_message}
EOI
    puts `git commit -m "#{message}"`
  else
    puts "Not Found"
  end

end

